"""
Django Admin configuration for charts app

Note: Models are unmanaged (managed=False), so no admin interface needed.
The database is managed by db_manager.py from the parent project.
"""

from django.contrib import admin

# No admin registration needed for unmanaged models
